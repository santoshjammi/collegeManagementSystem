# Prevent direct access to uploaded files
# Only allow access through PHP scripts
<Files "*">
    Order Deny,Allow
    Deny from all
</Files>

# Allow access to profile pictures (served through PHP)
<FilesMatch "\.(jpg|jpeg|png|gif)$">
    Order Allow,Deny
    Allow from all
</FilesMatch>